{
  "name": "kgs-proxy-server",
  "version": "1.0.0",
  "description": "Pure proxy server for KGS educational platform",
  "main": "api/[...path].js",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}